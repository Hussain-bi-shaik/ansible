- name: installing packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: installing packages
    anisble.builtin.package:
      name: "{{ item }}"
      state: installed
    loop:
    - git
    - zip
    - mysql  
